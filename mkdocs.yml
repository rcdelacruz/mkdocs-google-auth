site_name: MkDocs with Google Authentication
site_description: A MkDocs site with Google Authentication
site_author: Your Name

theme:
  name: material
  features:
    - navigation.instant
    - navigation.tracking
    - navigation.tabs
    - navigation.sections
    - navigation.expand
    - search.suggest
    - search.highlight
  palette:
    primary: indigo
    accent: indigo

plugins:
  - search
  - macros
  - simple:
      hooks:
        - mkdocs_google_auth.hooks.on_page_markdown
        - mkdocs_google_auth.hooks.on_pre_build
        - mkdocs_google_auth.hooks.on_pre_template

extra_javascript:
  - javascripts/auth.js

extra:
  generator: false
  auth:
    enabled: true
    allowed_domains:
      - gmail.com
      # Add more domains as needed
    allowed_emails:
      # Add specific emails here